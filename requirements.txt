# 多模态情绪识别系统 — 依赖（conda env: newcell）
# 安装: conda run -n newcell pip install -r requirements.txt

# 核心（torch 三件套固定为 env 现有版本，防止被依赖降级）
Django==5.2.15
torch==2.13.0
torchvision==0.28.0
torchaudio==2.11.0
numpy
opencv-python
facenet-pytorch
insightface==1.0.1
onnxruntime
transformers==5.14.*
huggingface_hub

# 未来阶段（已装于 newcell，列此以便移植）
reportlab
openai-whisper
speechbrain
pyserial
soundfile
